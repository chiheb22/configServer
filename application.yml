spring:
  boot:
    admin:
      client:
        url: http://localhost:9001
	instance
	  prefer-ip:true
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
      health:
	show-details:'always'
